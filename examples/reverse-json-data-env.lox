var input = getEnvVar("EVENT_INPUT");

var data = parse(input).data;

var reversedData = "";

for(var i = data.length() - 1; i >= 0; i = i - 1) {
  reversedData = reversedData + data.get(i);
}

perrorln("Reversed data: " + reversedData);

logln(stringify(.{
  data: reversedData
}));
