{
  var total = 0;
  logln("Start:", total);
  var start = clock();
  for(var i = 0; i <= 100000000; i = i + 1) {
    total = total + i;
  }
  var diff = clock() - start;

  logln("End:", total);
  logln("Time ", diff * 1000, "millseconds");
}